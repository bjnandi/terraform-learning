### In this terraform task I create network, instance, loadbalancher and DNS Zone for domain routing host static file on s3 bucket, and last we get nameserver & dns as a output which is point to domain. 
 
